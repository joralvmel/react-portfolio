"use client";

import styled from "styled-components";

const StyledProjectsGrid = styled.ul`
  a {
    position: relative;
    z-index: 1;
  }
`;

function Featured() {
  return <StyledProjectsGrid>Featured</StyledProjectsGrid>;
}

export default Featured;
